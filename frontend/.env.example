# Frontend Environment Variables

# Backend API URL (server-side, read at runtime by middleware)
API_URL=http://localhost:8000

# Enable preview feature (experimental)
NEXT_PUBLIC_ENABLE_PREVIEW=true
